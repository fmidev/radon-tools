#
# SConscript for neons-tools
#

Import('env')
import os

env.Program(target = 'grid_to_neons', source = ['main/grid_to_neons.cpp', 'source/BDAPLoader.cpp', 'source/NetCDFLoader.cpp', 'source/GribLoader.cpp', 'source/S3GribLoader.cpp'])
